<%= form_with(model: @murmur, local: true) do |form| %>

  <div class="murmur_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
<%= link_to 'つぶやき一覧画面に戻る', murmurs_path %>
