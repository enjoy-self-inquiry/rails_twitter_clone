<h3>以下の内容で、つぶやく。</h3>
<%= form_with(model: @murmur, url: murmurs_path, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>つぶやき:<%= @murmur.content %></p>
  <%= form.submit  "つぶやきを登録する" %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>
