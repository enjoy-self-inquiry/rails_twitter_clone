<h1>つぶやき一覧</h1>
<table>
  <tr>
    <th>つぶやき</th>
  </tr>
<% @murmurs.each do |murmur| %>
  <tr>
    <td><%= murmur.content %></td>
    <td><%= link_to "つぶやきを編集する", edit_murmur_path(murmur.id) %></td>
  </tr>
<% end %>
</table>

<%= link_to 'つぶやく', new_murmur_path %>
